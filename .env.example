# Copy this file to .env and fill in the values.
# .env is gitignored — never commit real credentials.

# HTTP basic auth for htaccess-protected environments (dev, test, uat)
HTACCESS_USER=esc
HTACCESS_PASS=axl-esc

# Set to '1' to run tests in a visible browser window
HEADED=
